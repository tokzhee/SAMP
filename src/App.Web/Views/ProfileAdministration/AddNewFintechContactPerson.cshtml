@model App.Web.ViewModels.CreateFintechContactPersonViewModel

@{
    ViewBag.Title = "Add New Fintech Contact Person";
}

<div class="row mb-4">
    <div class="col-12 d-flex align-items-center justify-content-between">
        <h5 class="page-title">Add New Fintech Contact Person to @Model.FintechName</h5>
    </div>
</div>

<div class="row">
    <div class="col-md-8 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                @if (TempData["Alert"] != null)
                {
                    @Html.Partial("_Alert", TempData["Alert"])
                }

                <form class="forms-sample" action="@Url.Action("AddNewFintechContactPerson")" method="post">
                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(model => model.FintechUrlQueryString)
                    @Html.Partial("_Person", Model.PersonModel)
                    <a href="@Url.Action("ViewFintechs")" class="btn btn-primary btn-xs mr-2">
                        <i class="mdi mdi-refresh"></i>
                        Back
                    </a>
                    <button type="submit" class="btn btn-success btn-xs mr-2">
                        <i class="mdi mdi-file-document"></i>
                        Submit
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

@section CustomScripts
{
    <script type="text/javascript">
        $(document).ready(function () {
            $("#divQueryString").hide();
        });
    </script>
}